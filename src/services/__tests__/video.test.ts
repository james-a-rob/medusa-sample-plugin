
// import { IdMap, MockManager, MockRepository } from "medusa-test-utils"

import VideoService from '../video';

// manual mocking. Replace with official Medusa test tooling once available

describe('video plugin service', () => {
    it('stores video', async () => {
        // const videoService = new VideoService({ videoRepository: { list: jest.fn() } });
        // await videoService.list();
        expect(1).toBe(2);
    });

    it('retrieves a video', () => {
        // return in correct yt embed format
    });
});